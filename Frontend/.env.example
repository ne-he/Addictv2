# URL of the deployed FastAPI backend, NO trailing slash.
# After deploying to Hugging Face Spaces it looks like:
#   https://<username>-addictv2-api.hf.space
# Read server-side by app/api/predict/route.js (never exposed to the browser).
API_URL=
